// Stagehand singleton session manager
// Map<sessionId, Stagehand> with lazy init, idle cleanup (5 min), URL cache (30s TTL)
// TODO: Phase 1 — implement getStagehand, getCachedScreenshot, updateCache

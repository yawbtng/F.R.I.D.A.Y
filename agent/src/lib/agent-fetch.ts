// Shared fetch utility for agent → Next.js API calls
// Handles: auth (JWT), abort (interrupt & pivot), timeout (10s), retries (max 2)
// TODO: Phase 2 — implement agentFetch + agentFetchWithHeartbeat
